# Intent

Reconcile `core.fct_trip` (target) against `core.fct_trip_baseline_agg` (baseline) in the
`nyctaxidomain` project, using the `reconciling-target-against-baseline` skill. See
`design.md` for the confirmed contract and pair artifact.
