select
    trip_key,
    pickup_date_key,
    daypart_code,
    service_type,
    pickup_zone_key,
    dropoff_zone_key,
    vendor_key,
    payment_type_key,
    rate_code_key,
    is_quality_flagged,
    is_fleet_attributed,
    assignment_method,
    pickup_datetime,
    dropoff_datetime,
    pickup_location_id,
    dropoff_location_id,
    passenger_count,
    store_and_fwd_flag,
    trip_distance,
    trip_seconds,
    avg_mph,
    fare_amount,
    extra,
    mta_tax,
    tip_amount,
    tolls_amount,
    improvement_surcharge,
    congestion_surcharge,
    total_surcharges,
    total_amount,
    fare_residual,
    tip_rate
from {{ ref('int_trip_enriched') }}
